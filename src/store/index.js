import { createStore } from 'vuex';
import userModule from './user.module';

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: { userModule },
  strict: true,
});
